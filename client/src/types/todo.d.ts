export type Todo = {
    id: number;
    title: string;
    context: string;
    limit_date: string;
    created_at: string;
    updated_at: string;
};

export type Form = {
    title: string;
    context: string;
    limit_date: string;
};
